[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "midi-render-pipeline"
version = "0.3.15"
description = "A lightweight MIDI-to-audio rendering pipeline with SFZ and embedded FluidSynth backends."
requires-python = ">=3.11"
dependencies = [
  "mido>=1.3",
  "numpy>=1.24",
  "soundfile>=0.12",
  "pysfizz>=0.1.3",
]

[project.scripts]
midi-render = "midi_render.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["midi_render*"]
